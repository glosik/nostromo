import { HovercraftDirective } from './hovercraft.directive';

describe('HovercraftDirective', () => {
  it('should create an instance', () => {
    const directive = new HovercraftDirective();
    expect(directive).toBeTruthy();
  });
});
