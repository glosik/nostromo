import { ReactiveStickyHeaderDirective } from './reactive-sticky-header.directive';

describe('ReactiveStickyHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveStickyHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
