import { SimpleScrollDirective } from './simple-scroll.directive';

describe('SimpleScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleScrollDirective();
    expect(directive).toBeTruthy();
  });
});
